/*
 1. Write a functiom called make_avg() which will take an array of integer
    and the size of that array and return the average of those values. 
    and return the average of those values.
*/

// Write your program here.

// ###### Solution ########

// Initial Variables
/*
let myArr = [79, 69, 70, 90, 99, 80];
let sum = 0;
let lenght = myArr.length;

for (let i = 0; i < myArr.length; i++) {
     sum += myArr[i];
}

function make_avg(sum, lenght) {
    let average = sum / lenght
    return average;
};

const myResult = make_avg(sum,lenght);

console.log(myResult);
*/